import { GameGuard } from './game.guard';
import { PlayerService } from './player.service';

export { GameGuard } from './game.guard';
export { PlayerService } from './player.service';

export const services = [PlayerService, GameGuard];
