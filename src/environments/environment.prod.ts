import { version } from '../../package.json';

export const environment = {
  production: true,
  local: false,
  siteUrl: '',
  apiUrl: '',
  version,
  auto0: {
    domain: '',
    clientId: '',
    audience: '',
  },
};
