import { GameService } from './game.service';
import { GameGuard } from './game.guard';

export { GameService } from './game.service';
export { GameGuard } from './game.guard';

export const services = [GameService, GameGuard];
