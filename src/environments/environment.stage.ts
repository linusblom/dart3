import { version } from '../../package.json';

export const environment = {
  production: false,
  local: false,
  siteUrl: '',
  apiUrl: '',
  version,
  auto0: {
    domain: '',
    clientId: '',
    audience: '',
  },
};
